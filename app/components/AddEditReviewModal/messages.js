/*
 * ShadowBox Messages
 *
 * This contains all the text for the ShadowBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ShadowBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ShadowBox component!',
  },
});
